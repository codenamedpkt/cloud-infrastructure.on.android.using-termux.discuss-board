name: Test Workflow
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2

      - name: Test
        run: |
           [[ $termux_discuss_here == "Abcde" ]] && echo "Oh no!" || echo "Yes! Secret got."
           cd $(dirname $0)
           echo $(dirname $0)
        env:
           termux_discuss_here: ${{ secrets.MY_TOKEN }}
